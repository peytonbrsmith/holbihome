<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="holbihome.ProjectsPage"
             xmlns:controls="clr-namespace:holbihome.Controls"
             xmlns:local="clr-namespace:holbihome"
             Shell.NavBarIsVisible="false">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <controls:CardView CardTitle="Tracks" CardIcon="{x:Static local:IconFont.Folder}"
                           Grid.Row="0" Grid.Column="0" ></controls:CardView>

        <controls:CardView CardTitle="Projects" CardIcon="{x:Static local:IconFont.CodeBranch}"
                           Grid.Row="0" Grid.Column="1"></controls:CardView>

    </Grid>

</ContentPage>

