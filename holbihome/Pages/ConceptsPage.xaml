<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="holbihome.ConceptsPage"
             xmlns:controls="clr-namespace:holbihome.Controls"
             xmlns:local="clr-namespace:holbihome">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <controls:CardView CardTitle="Concepts" CardIcon="{x:Static local:IconFont.Book}"
                           Grid.Row="0" Grid.Column="0" ></controls:CardView>

        <controls:CardView Grid.Row="0" Grid.Column="1" CardIcon="{x:Static local:IconFont.BookOpen}"></controls:CardView>

    </Grid>

</ContentPage>

